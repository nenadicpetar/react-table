import { initializeApp } from "firebase/app"
import { getDatabase } from "firebase/database"

function StartFirebase() {
    //copy files form Project Overview -> Project settings -> Your apps -> npm
}

export default StartFirebase;
